MDScreen:
    RelativeLayout:
        id: main_screen_app
        padding: "10dp"
        FloatingButton:
            pos: root.width - self.width - dp(10),  self.height - dp(30)  # Set the position with 20-pixel margin